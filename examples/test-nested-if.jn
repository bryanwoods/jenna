# Test nested if inside match
type Option a = Some a | None

let test = (opt) ->
  match opt with
  | Some(x) -> if x > 0 then x else 0
  | None -> 0
  end

let result = test(Some(5))
let msg = print("Result: 5")
