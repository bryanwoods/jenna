# Factorial function in Jenna
let factorial = (n) ->
  if n == 0 then
    1
  else
    n * factorial(n - 1)

# Compute factorial of 10
let result = factorial(10)
let output = print(intToString(result))
